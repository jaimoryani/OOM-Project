package Project;

import javax.swing.*;
import java.awt.*;
public class threeDPanel extends JPanel {
    threeDPanel(){
        setLayout(new FlowLayout());
        JButton j1=new JButton();
        add(j1);
    }
}
